# Claude Instructions

## Build & Deploy

Always run `hugo -d docs` and commit the `docs/` output together with any source changes before pushing. GitHub Pages serves the site from the `docs/` folder.

## Commits

Use "Updates" as the commit message. No co-author attribution.
